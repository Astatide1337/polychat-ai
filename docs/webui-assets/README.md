# WebUI PR Evidence

This directory stores screenshots, video, and JSON summaries generated by:

```bash
npm run test:live:web
```

The live script starts `polychat web --no-open`, opens the served WebUI with Playwright, verifies health/status rendering, sends a temporary-mode chat request when a live model is available, checks the debug and MCP panels, and writes PR-ready evidence artifacts here.
